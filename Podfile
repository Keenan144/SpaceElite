# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
target 'CometFall' do
  use_frameworks!
  pod 'SwiftyBeaver', '~> 1.1'

  target 'CometFallTests' do
    inherit! :search_paths

  end

  target 'CometFallUITests' do
    inherit! :search_paths

  end

end

target 'CometFall-DEV' do
  use_frameworks!
  pod 'SwiftyBeaver', '~> 1.1'

end
